import { StyleSheet, View, Text } from 'react-native';

export default function Commmunity() {
  return (
    <View style={styles.screenContainer}>
      <Text>Commmunity</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  screenContainer: {
    flex: 1,
  },
});
